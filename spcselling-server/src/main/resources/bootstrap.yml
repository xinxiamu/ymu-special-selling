spring:
  application:
    name: eurekaserver
  cloud:
    config:
      uri: http://localhost:8888/
  profiles: 
    active: dev-server1 #激活的配置文件，更改这里部署三个server做集群
server:
  port: 2000
encrypt:
  failOnError: false