spring:
  application: 
    name: config-server

server:
  port: 8888

spring:
  application:
    name: config-server
  
spring: 
  cloud:
    config: 
      server: 
        git: 
          uri: https://github.com/xinxiamu/ymu-special-selling.git
          searchPaths: spcselling-config/spcselling-config-repo
          username: xinxiamu
          password: wnt*ZMT*701712  

spring: 
  profiles:
    active: native #读取resources下面的配置文件
      
      
    
        
          