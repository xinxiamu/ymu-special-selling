server:
  port: 8010

---
spring:
  application:
    name: service-sys-common

---
spring:
  cloud:
    config: #配置中心
      name: service-sys-common
      profile: dev
      label: master
      fail-fast: true
      discovery:
        enabled: true
        service-id: config-server
      username: mutou
      password: 701712

---
eureka: #服务注册
  instance:
#    prefer-ip-address: true
    hostname: service-sys-email
  client:
    service-url:
      defaultZone: http://eureka-server1:1111/eureka/,http://eureka-server2:1112/eureka/,http://eureka-server3:1113/eureka/

---
encrypt:
  fail-on-error: false

---
logging:
  config: classpath:log4j2.xml