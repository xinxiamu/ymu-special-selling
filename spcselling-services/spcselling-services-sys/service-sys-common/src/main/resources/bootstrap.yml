spring:
  application:
    name: service-sys-common

---
spring:
  cloud:
    config: #配置中心
      name: service-sys-common
      profile: dev
      label: master
      fail-fast: true
      discovery:
        enabled: true
        service-id: config-server
      username: admin
      password: 123456
